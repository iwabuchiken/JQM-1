<h1>Listing settings_nr4s</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Sort genre</th>
    <th>Sort category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @settings_nr4s.each do |settings_nr4| %>
  <tr>
    <td><%= settings_nr4.id %></td>
    <td><%= settings_nr4.sort_genre %></td>
    <td><%= settings_nr4.sort_category %></td>
    <td><%= link_to 'Show', settings_nr4 %></td>
    <td><%= link_to 'Edit', edit_settings_nr4_path(settings_nr4) %></td>
    <td><%= link_to 'Destroy', settings_nr4, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Settings nr4', new_settings_nr4_path %>
