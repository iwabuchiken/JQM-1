<%= form_for(@setting_sl) do |f| %>
  <% if @setting_sl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting_sl.errors.count, "error") %> prohibited this setting_sl from being saved:</h2>

      <ul>
      <% @setting_sl.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :num_of_item_per_page %><br />
    <%= f.number_field :num_of_item_per_page %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
