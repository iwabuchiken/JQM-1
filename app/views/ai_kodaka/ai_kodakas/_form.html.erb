<%= form_for(@ai_kodaka) do |f| %>
  <% if @ai_kodaka.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ai_kodaka.errors.count, "error") %> prohibited this ai_kodaka from being saved:</h2>

      <ul>
      <% @ai_kodaka.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :line %><br />
    <%= f.text_area :line %>
  </div>
  <div class="field">
    <%= f.label :cut_morph %><br />
    <%= f.text_area :cut_morph %>
  </div>
  <div class="field">
    <%= f.label :page %><br />
    <%= f.number_field :page %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
