<%= form_for(@key_word_set) do |f| %>
  <% if @key_word_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@key_word_set.errors.count, "error") %> prohibited this key_word_set from being saved:</h2>

      <ul>
      <% @key_word_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :genre %><br />
    <%= f.text_field :genre %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :keywords %><br />
    <%= f.text_area :keywords %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
